"""
NOTE: Coqui TTS (the original XTTS v2 library) requires Python 3.9–3.12.
You are running Python 3.14, which is not supported.

Use the Edge TTS generator instead — same quality, no Python version limit:
    python scripts/generate_edge_tts.py

Edge TTS uses Microsoft's neural voices (same engine as Azure TTS),
is completely free, needs no API key, and works on any Python version.
"""

print(__doc__)
